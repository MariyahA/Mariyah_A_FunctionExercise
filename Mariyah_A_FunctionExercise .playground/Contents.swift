import UIKit
/*Function creates a place holder for the numbers of choice
and returns the sum of the numbers added together
*/
func add(num1: Int, num2: Int) -> Int{
    return num1 + num2
}
//Prints the sum of the numbers
print(add(num1: 40, num2: 50))
